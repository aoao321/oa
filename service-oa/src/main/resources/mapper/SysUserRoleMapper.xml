<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aoao.mapper.SysUserRoleMapper">


    <insert id="insertBatch">
        INSERT INTO sys_user_role (user_id, role_id)
            VALUES
                <foreach collection="userRoleList" separator="," item="item">
                    (#{item.userId}, #{item.roleId})
                </foreach>
    </insert>
    <select id="selectRoleNamesByUserId" resultType="java.lang.String">
        SELECT r.role_name
        FROM sys_user_role ur
                 JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
          AND ur.is_deleted = 0
    </select>
    <select id="selectRoleNamesByUserIds" resultType="java.util.Map">
        SELECT
        ur.user_id,
        r.role_name
        FROM
        sys_user_role ur
        LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE
        ur.is_deleted = 0
        AND ur.user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
